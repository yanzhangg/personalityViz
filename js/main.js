
/**
 * Load data from CSV file asynchronously and render charts
 */
 d3.csv('TODO.csv').then(data => {

    // Convert columns to numerical values
    data.forEach(d => {
      Object.keys(d).forEach(attr => {
      });
    });
  

  });